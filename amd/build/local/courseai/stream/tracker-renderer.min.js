define("local_coursegen/local/courseai/stream/tracker-renderer",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.renderGenerationTracker=void 0;
/**
   * Generation progress renderer.
   *
   * The generation phase reuses the SAME plan cards as planning (#prvSections)
   * instead of a separate progress panel, so the whole wizard stays visually
   * unified. This maps the generation tracker's per-activity status onto the real
   * activity rows (matched by data-activity-id): a status class drives a spinner
   * while an activity is being created in Moodle, then a check when it is done
   * (see the body.cg-generating rules in aicoursecreation.css).
   *
   * @module     local_coursegen/local/courseai/stream/tracker-renderer
   * @copyright  2026 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const STATUS_CLASS={done:"cg-gen-done",in_progress:"cg-gen-active",pending:"cg-gen-pending"},ALL_STATUS_CLASSES=["cg-gen-done","cg-gen-active","cg-gen-pending"];_exports.renderGenerationTracker=state=>{const tracker=state.generationTracker;if(!tracker||!Array.isArray(tracker.flat))return;const root=document.getElementById("prvSections");root&&tracker.flat.forEach((activity=>{if(!activity||!activity.id)return;const row=root.querySelector('[data-activity-id="'+activity.id+'"]');row&&(row.classList.remove(...ALL_STATUS_CLASSES),row.classList.add(STATUS_CLASS[activity.status]||"cg-gen-pending"))}))}}));

//# sourceMappingURL=tracker-renderer.min.js.map
define("local_coursegen/local/courseai/template/preview",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.usePreviewSession=_exports.refreshPreviewLinks=void 0;
/**
   * The links that open what a generation is going to produce.
   *
   * Both previews read one run's answer, so neither exists before a run does:
   * the controls are in the page from the start and stay hidden until a session
   * is opened, rather than being built at that moment, so the structure is
   * rendered once and never rebuilt just to add a link to it.
   *
   * @module     local_coursegen/local/courseai/template/preview
   * @copyright  2026 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
let sessionId=0;_exports.usePreviewSession=id=>{sessionId=Number(id)||0,refreshPreviewLinks()};const refreshPreviewLinks=()=>{const course=document.getElementById("tplPreviewCourse");course&&(course.hidden=sessionId<=0,course.href=sessionId>0?M.cfg.wwwroot+"/local/coursegen/course_preview.php?sessionid="+sessionId:"#"),document.querySelectorAll('[data-action="local_coursegen/template/preview-activity"]').forEach((link=>{const uid=link.dataset.generationUid;link.hidden=sessionId<=0||!uid,link.href=sessionId>0&&uid?M.cfg.wwwroot+"/local/coursegen/activity_preview.php?sessionid="+sessionId+"&uid="+encodeURIComponent(uid):"#",link.target="_blank",link.rel="noopener"}))};_exports.refreshPreviewLinks=refreshPreviewLinks}));

//# sourceMappingURL=preview.min.js.map
define("local_coursegen/local/courseai/template/course_preview",["exports","core/templates","core/str","core/notification"],(function(_exports,_templates,_str,_notification){var obj;
/**
   * What the course preview adds to the page its format drew.
   *
   * The page is a real course page, rendered by the course's own format, and it
   * is left exactly as the format drew it. Two things still have to happen to
   * it: the activities the run is going to write have to appear in the sections
   * they will be written into, and following a link has to stay inside the
   * preview instead of landing on the template's real course.
   *
   * Both are done here rather than to the markup on the way out. A rendered
   * course is a document, and a document knows where its own elements are and
   * where they end; deciding that again by reading the markup as text means
   * counting tags by hand, and getting it wrong shows up as a page that is
   * subtly not the page.
   *
   * @module     local_coursegen/local/courseai/template/course_preview
   * @copyright  2026 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_templates=(obj=_templates)&&obj.__esModule?obj:{default:obj};_exports.init=(sessionId,courseId,planned)=>{keepLinksInsidePreview(sessionId,courseId),keepActivitiesInsidePreview(sessionId),planned.forEach((section=>addPlannedActivities(section))),openSectionsThatShowOnlyTheirSummary(sessionId)};const openSectionsThatShowOnlyTheirSummary=sessionId=>{document.querySelectorAll("[data-section][data-sectiontitle]").forEach((block=>{if(block.querySelector('[data-for="cmlist"]')||block.querySelector(".local-coursegen-open-section"))return;const url=new URL(M.cfg.wwwroot+"/local/coursegen/course_preview.php");url.searchParams.set("sessionid",sessionId),url.searchParams.set("section",block.dataset.section);const link=document.createElement("a");link.className="btn btn-secondary local-coursegen-open-section",link.href=url.toString(),block.appendChild(link),(0,_str.get_string)("courseai_preview_open_section","local_coursegen").then((label=>(link.textContent=label,null))).catch(_notification.exception)}))},keepActivitiesInsidePreview=sessionId=>{document.querySelectorAll('a[href*="/mod/"]').forEach((link=>{let url;try{url=new URL(link.getAttribute("href"),window.location.origin)}catch(error){return}const cmid=url.searchParams.get("id");if(!url.pathname.endsWith("/view.php")||!url.pathname.includes("/mod/")||!cmid)return;const preview=new URL(M.cfg.wwwroot+"/local/coursegen/activity_preview.php");preview.searchParams.set("sessionid",sessionId),preview.searchParams.set("cmid",cmid),link.setAttribute("href",preview.toString())}))},keepLinksInsidePreview=(sessionId,courseId)=>{document.querySelectorAll("a[href], option[value]").forEach((element=>{const attribute="OPTION"===element.tagName?"value":"href",target=previewUrl(element.getAttribute(attribute),sessionId,courseId);null!==target&&element.setAttribute(attribute,target)}))},previewUrl=(href,sessionId,courseId)=>{if(!href)return null;let url;try{url=new URL(href,window.location.origin)}catch(error){return null}const preview=new URL(M.cfg.wwwroot+"/local/coursegen/course_preview.php");if(preview.searchParams.set("sessionid",sessionId),url.pathname.endsWith("/course/view.php")){if(Number(url.searchParams.get("id"))!==courseId)return null;const section=url.searchParams.get("section");return null!==section&&preview.searchParams.set("section",section),preview.toString()+url.hash}if(url.pathname.endsWith("/course/section.php")){const number=sectionNumberOf(url.searchParams.get("id"));return null===number?null:(preview.searchParams.set("section",number),preview.toString())}return null},sectionNumberOf=sectionId=>{if(!sectionId)return null;const section=document.querySelector('[data-id="'.concat(CSS.escape(sectionId),'"][data-sectionid]'));return section?section.dataset.sectionid:null},addPlannedActivities=section=>{const lists=document.querySelectorAll('[data-id="'.concat(CSS.escape(section.sectionid),'"] [data-for="cmlist"]'));lists.length&&section.activities.forEach((activity=>{_templates.default.renderForPromise("local_coursegen/preview_activity_row",activity).then((_ref=>{let{html:html}=_ref;return lists.forEach((list=>_templates.default.appendNodeContents(list,html,""))),null})).catch(_notification.exception)}))}}));

//# sourceMappingURL=course_preview.min.js.map
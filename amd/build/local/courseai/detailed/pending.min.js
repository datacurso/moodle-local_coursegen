define("local_coursegen/local/courseai/detailed/pending",["exports","./activity-dom","./container"],(function(_exports,_activityDom,_container){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.removeTransientSectionPlaceholders=_exports.removeTransientActivityPlaceholders=_exports.removeAllTransientPlaceholders=_exports.markProposalTargetPending=void 0;
/**
   * Client-driven pending-skeleton helpers for proposal apply.
   *
   * When the user applies a proposal, the SSE stream reopens in 'planning' mode
   * with keepPlan=true and re-streams the kept plan WITHOUT real UUIDs, so the
   * structural skeleton sync is suppressed (it would duplicate the kept rows).
   * The side effect is that nothing skeletons during apply — the affected element
   * just silently updates or appears.
   *
   * The client already knows EXACTLY what is being applied: the selected
   * proposal's intent carries action + target_ids + parent_section_id + position.
   * These helpers use that intent to put the loading skeleton on the right element
   * the moment apply is dispatched, independent of the chaotic re-stream:
   *   - replan_activity / replan_section: reopen the existing target row(s) so the
   *     incoming detailed_plan fill re-runs (markActivityPlanned bails on done).
   *   - add_activity / add_section: insert a TRANSIENT skeleton placeholder at the
   *     target position. The genuinely-new real-UUID row removes the matching
   *     transient when it is rendered (see removeTransientActivityPlaceholders /
   *     removeTransientSectionPlaceholders, called from the row factories), so a
   *     duplicate never lingers.
   *
   * @module     local_coursegen/local/courseai/detailed/pending
   * @copyright  2026 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const insertTransientActivity=(ctx,parentSectionId,position)=>{const{state:state}=ctx,meta=state.detailedSectionMeta[parentSectionId]||state.detailedSubsectionMeta&&state.detailedSubsectionMeta[parentSectionId],bodyEl=meta&&(meta.bodyEl||meta.listEl);if(!bodyEl)return;const wrap=(()=>{const wrap=document.createElement("li");wrap.className="activity activity-wrapper cg-transient-activity",wrap.setAttribute("data-cg-transient","activity");const item=document.createElement("div");item.className="activity-item focus-control cg-activity--pending";const grid=document.createElement("div");grid.className="activity-grid";const textDiv=document.createElement("div");return textDiv.className="activity-altcontent activity-description cg-activity-desc-slot",textDiv.style.display="none",grid.appendChild(textDiv),item.appendChild(grid),wrap.appendChild(item),(0,_activityDom.attachSkeletonProgress)(textDiv),wrap})(),rows=Array.prototype.filter.call(bodyEl.children,(el=>el.classList.contains("activity")&&!el.classList.contains("dp-add-activity-wrap"))),addWrap=bodyEl.querySelector(".dp-add-activity-wrap"),idx="number"==typeof position&&position>=0?position:rows.length,ref=idx<rows.length?rows[idx]:addWrap;ref?bodyEl.insertBefore(wrap,ref):bodyEl.appendChild(wrap)},insertTransientSection=(ctx,position)=>{const sectionList=(0,_container.getSectionList)(ctx);if(!sectionList)return;const row=(ctx=>{const{texts:texts}=ctx,row=document.createElement("li");row.className="section course-section main clearfix cg-transient-section",row.setAttribute("data-cg-transient","section");const sectionItem=document.createElement("div");sectionItem.className="section-item";const headerEl=document.createElement("div");headerEl.className="course-section-header d-flex align-items-center position-relative";const titleEl=document.createElement("h3");titleEl.className="h4 sectionname course-content-item d-flex align-items-center mb-0",titleEl.textContent=texts&&texts.courseai_generating_details||"",headerEl.appendChild(titleEl);const bodyEl=document.createElement("div");bodyEl.className="content course-content-item-content collapse show";const cmlistEl=document.createElement("ul");cmlistEl.className="section m-0 p-0 img-text d-block";const li=document.createElement("li");li.className="activity activity-wrapper";const item=document.createElement("div");item.className="activity-item focus-control cg-activity--pending";const grid=document.createElement("div");grid.className="activity-grid";const textDiv=document.createElement("div");return textDiv.className="activity-altcontent activity-description cg-activity-desc-slot",textDiv.style.display="none",grid.appendChild(textDiv),item.appendChild(grid),li.appendChild(item),cmlistEl.appendChild(li),bodyEl.appendChild(cmlistEl),(0,_activityDom.attachSkeletonProgress)(textDiv),sectionItem.appendChild(headerEl),sectionItem.appendChild(bodyEl),row.appendChild(sectionItem),row})(ctx),sections=Array.prototype.filter.call(sectionList.children,(el=>el.classList.contains("course-section"))),addWrap=sectionList.querySelector(".dp-add-section-wrap"),idx="number"==typeof position&&position>=0?position:sections.length,ref=idx<sections.length?sections[idx]:addWrap;ref?sectionList.insertBefore(row,ref):sectionList.appendChild(row)};_exports.markProposalTargetPending=(ctx,intent)=>{if(!intent||"string"!=typeof intent.action)return;const targetIds=Array.isArray(intent.target_ids)?intent.target_ids:[],position="number"==typeof intent.position?intent.position:null;switch(intent.action){case"replan_activity":case"replace_activity":targetIds.forEach((id=>(0,_activityDom.reopenActivityEntry)(ctx,id)));break;case"replan_section":targetIds.forEach((sectionId=>((ctx,sectionId)=>{const{state:state}=ctx;Object.keys(state.detailedActivityEls).forEach((activityId=>{const entry=state.detailedActivityEls[activityId];!entry||entry.sectionId!==sectionId&&entry.subsectionId!==sectionId||(0,_activityDom.reopenActivityEntry)(ctx,activityId)}))})(ctx,sectionId)));break;case"add_activity":intent.parent_section_id&&insertTransientActivity(ctx,intent.parent_section_id,position);break;case"add_section":insertTransientSection(ctx,position)}};_exports.removeTransientActivityPlaceholders=bodyEl=>{bodyEl&&bodyEl.querySelectorAll("[".concat("data-cg-transient",'="activity"]')).forEach((el=>el.remove()))};_exports.removeTransientSectionPlaceholders=ctx=>{const sectionList=(0,_container.getSectionList)(ctx);sectionList&&sectionList.querySelectorAll("[".concat("data-cg-transient",'="section"]')).forEach((el=>el.remove()))};_exports.removeAllTransientPlaceholders=()=>{document.querySelectorAll("[".concat("data-cg-transient","]")).forEach((el=>el.remove()))}}));

//# sourceMappingURL=pending.min.js.map
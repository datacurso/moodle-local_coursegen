define("local_coursegen/local/template/sections_events",["exports","./type_action_sync","./selection_bulk","./template_row_scope"],(function(_exports,_type_action_sync,_selection_bulk,_template_row_scope){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.resetSectionsDirtyState=_exports.bindServerRenderedControls=void 0;
/**
   * Event bindings for the server-rendered "Course sections" review controls.
   *
   * Bound again after EVERY render of the review (initial page load and each
   * AJAX course switch — see step_sections.js, the only caller), since a
   * course switch replaces the whole container's innerHTML. The controls:
   *
   * - Per-row action select (data-region="activity-action"): its rendered
   *   value is the server-side default, so binding first SEEDS
   *   state.activityAction from it (the server render is the single source of
   *   truth for defaults), then keeps state in sync on change. Changing it TO
   *   "template" opens the shared scope modal (template_scope_modal.js)
   *   instead of applying the action immediately; changing it away from
   *   "template" clears the row's visual signal with no modal involved.
   * - Per-row clickable "Template" tag (data-region="template-tag"): visible
   *   only while the row's action is "template", reopens the same modal to
   *   change an already-set scope.
   * - Per-row selection checkboxes (data-region="activity-select") plus the
   *   single global bulk select (data-region="bulk-action") — see
   *   selection_bulk.js, bound from here.
   * - Section behavior selects (data-region="section-behavior"): their
   *   rendered value is the server-side default (the saved behavior in edit
   *   mode), so binding seeds state.sectionBehavior from it and keeps it in
   *   sync on change.
   *
   * State is the live object owned by init.js — mutated directly, never
   * passed through setState() (whose selectedCourseId handling would re-run
   * the whole config region render on every click).
   *
   * @module     local_coursegen/local/template/sections_events
   * @copyright  2025 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
let dirty=!1;const markDirty=()=>{dirty||(dirty=!0,window.addEventListener("beforeunload",onBeforeUnload))},onBeforeUnload=e=>{e.preventDefault(),e.returnValue=""};_exports.resetSectionsDirtyState=()=>{dirty=!1,window.removeEventListener("beforeunload",onBeforeUnload)};const applicableAction=(action,modname)=>"modify"!==action&&"template"!==action||(0,_type_action_sync.typeSupportsModify)(modname)?action:"keep";_exports.bindServerRenderedControls=(container,state)=>{container.querySelectorAll('select[data-region="activity-action"]').forEach((select=>{const cmid=parseInt(select.dataset.id);if(!cmid)return;const row=select.closest('[data-for="cmitem"]');let prioraction=select.value;state.activityAction[cmid]=select.value,row&&(0,_template_row_scope.applyTemplateVisual)(row,"template"===select.value,cmid,state),select.addEventListener("change",(()=>{const action=select.value;if(state.activityAction[cmid]=action,!row)return prioraction=action,void markDirty();"template"===action?(0,_template_row_scope.openScopeModalForNewSelection)(row,select,cmid,prioraction,state,(finalaction=>{prioraction=finalaction})):(prioraction=action,(0,_template_row_scope.applyTemplateVisual)(row,!1,cmid,state)),markDirty()}))})),(0,_template_row_scope.bindTemplateTagClicks)(container,state,markDirty),(0,_selection_bulk.bindSelectionAndBulk)(container,state,{applicableAction:applicableAction,applyTemplateVisual:_template_row_scope.applyTemplateVisual,markDirty:markDirty}),container.querySelectorAll('select[data-region="section-behavior"]').forEach((select=>{const sid=parseInt(select.dataset.sid);sid&&(state.sectionBehavior[sid]=select.value,select.addEventListener("change",(()=>{state.sectionBehavior[sid]=select.value,markDirty()})))}))}}));

//# sourceMappingURL=sections_events.min.js.map
define("local_coursegen/local/template/step_sections",["exports","./repository","./type_action_sync","./sections_events","core/notification"],(function(_exports,_repository,_type_action_sync,_sections_events,_notification){var obj;
/**
   * Section/activity structure review — renders the "Course sections" view
   * already built server-side (see classes/output/sections_config.php), for
   * both the initial page load and the AJAX course-selection path
   * (get_course_preview always runs through the same server-side renderer, so
   * there is only ONE place that builds this markup, not a client-side copy
   * that can drift from it).
   *
   * After each render (either path) sections_events.js binds the review's
   * controls — row action selects, selection checkboxes, bulk action bars and
   * section behavior selects — so their changes flow into the shared state that
   * buildSections() turns into the save payload. The defaults flow the same
   * way as before: init.js::initSectionState() seeds
   * state.sectionBehavior/activityAction/activityRef/activityPrompt from the
   * course structure, applyTypeDefaultsToState() below re-seeds from the
   * rendered rows' data-modname as a safety net, and binding then re-seeds
   * activityAction from the rendered selects themselves (the server-side
   * render is the single source of truth for per-row defaults).
   *
   * @module     local_coursegen/local/template/step_sections
   * @copyright  2025 Wilber Narvaez <https://datacurso.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.resetSectionsRender=_exports.renderStepSections=void 0,_notification=(obj=_notification)&&obj.__esModule?obj:{default:obj};let rendered=!1;_exports.resetSectionsRender=()=>{rendered=!1};_exports.renderStepSections=async(panel,state,isFreshFromPageLoad)=>{if(rendered)return;let container=panel.querySelector('[data-region="sections-config"]');if(isFreshFromPageLoad&&container&&container.querySelector('[data-region="section-behavior"], [data-region="activity-action"]'))return(0,_type_action_sync.applyTypeDefaultsToState)(container,state),(0,_sections_events.bindServerRenderedControls)(container,state),void(rendered=!0);container=panel.querySelector('[data-region="sections-config"]')||panel,container.innerHTML='<div class="d-flex align-items-center py-5 justify-content-center">\n            <div class="spinner-border text-primary mr-2" role="status"></div>\n            <span class="text-muted">Loading course structure...</span>\n        </div>';try{const preview=await(0,_repository.getCoursePreview)(state.selectedCourseId,state.templateId||0);container.innerHTML=preview.html,(0,_type_action_sync.applyTypeDefaultsToState)(container,state),(0,_sections_events.bindServerRenderedControls)(container,state),rendered=!0}catch(e){_notification.default.exception(e)}}}));

//# sourceMappingURL=step_sections.min.js.map